# Phaser Typescript Webpack Project Template

A template project for quickly spinning up Phaser with Typescript.

Is this a good idea? Probably not :P

## Usage

Start the development server:

```sh
npm start
```

Build the project:

```sh
npm run build
```
